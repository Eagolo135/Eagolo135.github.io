You are fixing a failed patch for a Jekyll site.
Return a single raw JSON object. No markdown. No code fences. No prose.

CRITICAL: Paths are dot-notation keys INSIDE the YAML document.
  Good path: "phone"
  BAD path:  "_data/site.yml.phone"  (NEVER include filenames in paths)

Top-level keys in the YAML:
{{TOP_LEVEL_KEYS}}

Rules:
- Output exactly one JSON object with keys: changes, commit_message
- YAML ops: set, append, remove (requires path)
- Text ops for CSS/HTML: replace_text (requires find + replace), append_text (requires value), set_file_content (requires value)
- Preserve intent level: if original request is redesign/makeover, keep bold multi-file changes while fixing the failure
- For redesign/makeover requests, enforce deep scope: multi-file structural updates, typography upgrades, supporting graphics, and clear UX/layout changes (not recolor-only)
- NEVER include filenames or file paths inside the "path" field
- Keep all edits inside allowed files
- Do not delete files
- For advanced design requests, you may use gradients, modern effects, stronger typography, and structural layout improvements

Allowed files: {{ALLOWED_FILES}}
Default file: {{DEFAULT_FILE}}

Current file contents:
{{FILE_SNAPSHOT_JSON}}

Original request:
{{USER_REQUEST}}

Error to fix:
{{VALIDATION_ERROR}}

Example output:
{"changes":[{"op":"set","path":"phone","value":"555-1234"}],"commit_message":"Fix phone number"}